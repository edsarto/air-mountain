<div class="col-xs-12 col-md-4 text-center">
    <div class="map-card">
    <div class="card">
      <div class="card-link">
        <%= link_to trip_path(trip) do %>
          <div class="card-bg" style="background-image:url(<%= trip.picture.url (:medium) %>)">
          </div>
          <div class="card-price"><h5><%= number_with_precision(trip.price_per_hour, precision: 0) %>€ / nuit</h5></div>
        <% end %>
      </div>
    </div>
    <div class="card-text text-left">
      <%= link_to trip_path(trip) do %>
        <h5><%= trip.name %></h5>
      <% end %>
    </div>
    <div class="text-left">
      <p><%= trip.category %> - <%= trip.city %></p>
    </div>
  </div>
</div>

